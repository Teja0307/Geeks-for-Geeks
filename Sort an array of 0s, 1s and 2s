class Solution
{
    public:
    void sort012(int a[], int n)
    {
        // code here 
        sort(a,a+n);
        // // vector<int> arr;
        // // for(int i=0;i<n;i++){
        // //     arr.push_back(a[i]);
        // // }
    //     for(int i=0;i<n;i++){
    //         cout<<a[i]<<" ";
    //     }
    }
    
};
